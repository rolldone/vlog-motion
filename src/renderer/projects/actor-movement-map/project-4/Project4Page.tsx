import { MapAdminPage } from './MapAdminPage'

/**
 * Project4Page — thin wrapper.
 * All state & logic lives in MapAdminPage (admin panel + map view).
 * MapView is a pure component that can be used standalone in gameplay fullscreen.
 */
export function Project4Page() {
  return <MapAdminPage />
}

